package BFSDFS;

public class RotttingOrange {
    public int orangesRotting(int[][] grid) {
        int row = grid.length;
        int col = grid[0].length;
        if (row > 10 || col > 10)
            return

        int[] rotten = new int[row*col];

        for(int rotten)
    }
    public static void main(String args[]) {

    }
}
